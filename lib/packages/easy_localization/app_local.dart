import 'package:flutter/material.dart';

class AppLocal {
  static const Locale teluguLocale = Locale('te', 'IN');
  static const Locale hindiLocale = Locale('hi', 'IN');
  static const Locale englishLocale = Locale('en', 'UK');
  static const Locale kannadaLocale = Locale('kn', 'IN');

  static const String localePath = 'assets/translations';
}
