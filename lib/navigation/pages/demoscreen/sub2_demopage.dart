import 'package:flutter/material.dart';

class Sub2DemoPage extends StatelessWidget {
  const Sub2DemoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Sub2 Demo Page'),
      ),
    );
  }
}
