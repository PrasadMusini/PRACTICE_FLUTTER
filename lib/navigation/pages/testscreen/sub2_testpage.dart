import 'package:flutter/material.dart';

class Sub2TestPage extends StatelessWidget {
  const Sub2TestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Sub2 Test Page'),
      ),
    );
  }
}
